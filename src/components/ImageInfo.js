import React from 'react';

const ImageInfo = ({ sentence }) => ( <
    p > { sentence } < /p>
    // <
    // div className = "row info" >
    // <
    // div className = "col-xs-12" >
    // <
    // p className = "sentence" > { sentence } < /p> < /
    // div > <
    // /div> 
    // /
    // div >
);


export default ImageInfo;